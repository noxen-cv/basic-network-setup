# Infrastructure Folder

Use this folder for Infrastructure as Code implementations of the same network design:

- `bicep/` for Azure Bicep templates.
- `terraform/` for Terraform modules and stacks.

The Bash reference implementation is in `../scripts/bash/`.
